meta {
  name: Signin
  type: http
  seq: 1
}

post {
  url: {{url}}/auth/signin
  body: json
  auth: inherit
}

body:json {
  {
    "username": "Publisher1",
    "password": "{{password}}"
  }
}

settings {
  encodeUrl: true
}
